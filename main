var roleHarvester = require('role.harvester');
var roleUpgrader = require('role.upgrader');
var roleBuilder = require('role.builder');
var roleMiner = require('role.miner');

module.exports.loop = function () {
    
    for(var name in Memory.creeps) {
        if(!Game.creeps[name]) {
            delete Memory.creeps[name];
            console.log('Clearing non-existing creep memory:', name);
        }
    }
    
    var harvesters = _.filter(Game.creeps, (creep) => creep.memory.role == 'harvester');
    var builders = _.filter(Game.creeps, (creep) => creep.memory.role == 'builder');
    var upgraders = _.filter(Game.creeps, (creep) => creep.memory.role == 'upgrader');
    
    if(harvesters.length < 2) {
        var newName = 'Harvester' + Game.time;
        console.log('Spawning new harvester: ' + newName);
        Game.spawns['Spawn1'].spawnCreep([WORK,CARRY,MOVE], newName, 
            {memory: {role: 'harvester'}});
    }
    
    if(builders.length < 2) {
        var newName = 'Builder' + Game.time;
        console.log('Spawning new builder: ' + newName);
        Game.spawns['Spawn1'].spawnCreep([WORK,CARRY,MOVE], newName, 
            {memory: {role: 'builder'}});
    }
    
    if(upgraders.length < 2) {
        var newName = 'Upgrader' + Game.time;
        console.log('Spawning new upgrader: ' + newName);
        Game.spawns['Spawn1'].spawnCreep([WORK,CARRY,MOVE], newName, 
            {memory: {role: 'upgrader'}});
    }
    
    var pathsToSources = [];
    var sources = Game.spawns['Spawn1'].room.find(FIND_SOURCES);
    
    var buildQueue = [];
    var maxConSites = 3;
    
    console.log("Old: " + Game.spawns['Spawn1'].memory.buildQueue.length);
    if (Game.spawns['Spawn1'].room.controller.level >= 2) {
        for (var a in sources) {
            pathsToSources.push(PathFinder.search(Game.spawns['Spawn1'].pos, {pos: sources[a], range: 1}));
        }
        for (var b in pathsToSources) {
            buildPath(pathsToSources[b].path, STRUCTURE_CONTAINER);
        }
    }
    
    function buildPath (path, end) {
        for(var a in path) {
            if (a < path.length) {
                Game.spawns['Spawn1'].memory.buildQueue.push(path[a].x, path[a].y, STRUCTURE_ROAD);
            }
            if (a == path.length) {
                Game.spawns['Spawn1'].memory.buildQueue.push(path[a].x, path[a].y, end);
            }
        }
    }
    
    console.log("New: " + Game.spawns['Spawn1'].memory.buildQueue.length);
    console.log(Game.spawns['Spawn1'].room.find(FIND_CONSTRUCTION_SITES).length);
    
    if (Game.spawns['Spawn1'].memory.buildQueue.length > 0 && Game.spawns['Spawn1'].room.find(FIND_CONSTRUCTION_SITES).length < maxConSites) {
        Game.spawns['Spawn1'].room.createConstructionSite(Game.spawns['Spawn1'].memory.buildQueue[0].x, Game.spawns['Spawn1'].memory.buildQueue[0].y, Game.spawns['Spawn1'].memory.buildQueue[0].type);
        Game.spawns['Spawn1'].memory.buildQueue.shift();
    }
    
    /* Restructure this to draw a path to sources, build miner, and build a container */
    /*
    var miners = _.filter(Game.creeps, (creep) => creep.memory.role == 'miner');
    var sources = Game.spawns['Spawn1'].room.find(FIND_SOURCES);
    
    if (miners.length < sources.length) {
        for (var a in sources) {
            var mined = false;
            var curSourceId = sources[a].id;
            for (var b in miners) {
                if (Game.getObjectById(miners[b].memory.sourceId).id == curSourceId) {
                    mined = true;
                }
            }
            
            if (!mined) {
                if(!Game.spawns['Spawn1'].spawning) {
                    var newName = "Miner" + Game.time;
                    console.log('Spawning new miner: ' + newName);
                    Game.spawns['Spawn1'].spawnCreep([WORK,MOVE], newName, 
                        {memory: {role: 'miner', sourceId: curSourceId}});
                }
            }
        }
    }
    */
    if(Game.spawns['Spawn1'].spawning) { 
        var spawningCreep = Game.creeps[Game.spawns['Spawn1'].spawning.name];
        Game.spawns['Spawn1'].room.visual.text(
            '🛠️' + spawningCreep.memory.role,
            Game.spawns['Spawn1'].pos.x + 1, 
            Game.spawns['Spawn1'].pos.y, 
            {align: 'left', opacity: 0.8});
    }
    
    for(var name in Game.creeps) {
        var creep = Game.creeps[name];
        if(creep.memory.role == 'harvester') {
            roleHarvester.run(creep);
        }
        if(creep.memory.role == 'upgrader') {
            roleUpgrader.run(creep);
        }
        if(creep.memory.role == 'builder') {
            roleBuilder.run(creep);
        }
        if(creep.memory.role == 'miner') {
            roleMiner.run(creep);
        }
    }
}
